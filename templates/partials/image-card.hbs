<article class="mta-image-card {{#if active}}is-active{{/if}}" data-image-id="{{id}}" data-image-type="{{imageType}}"
  data-index="{{idx}}">
  <div class="mta-card-clickable" data-action="select-image">
    <img src="{{src}}" alt="{{imageType}} image {{idx}}" />
    {{#if active}}<span class="mta-active-dot"></span>{{/if}}
    {{#if isDefault}}<small class="mta-default-label">default</small>{{/if}}
  </div>
  <button type="button" class="mta-edit-btn" data-action="open-settings" aria-label="Open settings">🖊</button>
  <button type="button" class="mta-delete-btn" data-action="delete-from-card" aria-label="Delete">🗑️</button>
</article>