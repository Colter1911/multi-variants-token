# Multi Token Art — Техническая спецификация модуля для Foundry VTT v13

## 1. Общее описание

**Название модуля:** `multi-tokenart`  
**Совместимость:** Foundry VTT v13+ (строго)  
**System-agnostic:** Да  
**Назначение:** Управление множественными изображениями токена и портрета актёра с системой автоматического переключения по HP, кастомным скриптам, random-режимом и интеграцией с Dynamic Token Ring.

## 2. Архитектура данных

### 2.1 Гибридное хранение (Actor + Token)
- `actor.flags['multi-tokenart']` — источник истины, библиотека изображений и настроек.
- `token.flags['multi-tokenart']` — только активные IDs и служебные данные токена.

### 2.2 Структура данных
- `version`, `global` (`autoRotate`, `tokenRandom`, `portraitRandom`)
- `tokenImages[]` и `portraitImages[]` (до 100)
- для token images есть `dynamicRing`
- для token flags: `activeTokenImageId`, `activePortraitImageId`, `originalRing`

### 2.3 HP-атрибут (System-Agnostic)
- `game.settings('multi-tokenart', 'hpCurrentPath')`
- `game.settings('multi-tokenart', 'hpMaxPath')`
- автоопределение пресетов при первом запуске с возможностью ручной настройки.
---

## 2. Архитектура данных

### 2.1 Гибридное хранение (Actor + Token)

**Actor flags** (`actor.flags['multi-tokenart']`) — библиотека всех изображений и их настроек. Является «источником истины». Default-изображения автоматически применяются при создании нового токена.

**Token flags** (`token.flags['multi-tokenart']`) — хранит только ID текущих активных изображений для этого конкретного токена. Позволяет разным токенам одного актёра иметь разный выбор.

### 2.2 Структура данных

```jsonc
// actor.flags['multi-tokenart']
{
  "version": 1,

  // Глобальные настройки
  "global": {
    "autoRotate": true,          // Поворот на 270° при HP <= 0
    "tokenRandom": false,        // Random-режим для токенов
    "portraitRandom": false       // Random-режим для портретов
  },

  // Библиотека изображений токена
  "tokenImages": [
    {
      "id": "abc123",              // nanoid / foundry.utils.randomID()
      "src": "multi-tokenart/ActorName/token1.webp",
      "sort": 0,                   // Порядок сортировки (drag-n-drop)
      "isDefault": true,           // Default-изображение
      "autoEnable": {
        "wounded": false,
        "woundedPercent": 50,      // % от max HP
        "die": false               // HP <= 0
      },
      "customScript": "",          // JS-код (строка), контекст: actor, token
      "dynamicRing": {
        "enabled": false,
        "scaleCorrection": 1.0,
        "ringColor": "#000000",
        "backgroundColor": "#000000"
      }
    }
    // ... до 100 записей
  ],

  // Библиотека изображений портрета
  "portraitImages": [
    {
      "id": "def456",
      "src": "multi-tokenart/ActorName/portrait1.webp",
      "sort": 0,
      "isDefault": true,
      "autoEnable": {
        "wounded": false,
        "woundedPercent": 50,
        "die": false
      },
      "customScript": ""
      // Нет autoRotate и dynamicRing для портретов
    }
    // ... до 100 записей
  ]
}
```

```jsonc
// token.flags['multi-tokenart']
{
  "activeTokenImageId": "abc123",     // ID выбранного token image
  "activePortraitImageId": "def456"   // ID выбранного portrait image
}
```

### 2.3 HP-атрибут (System-Agnostic)

Для поддержки разных систем, путь к HP определяется через настройки модуля:

```jsonc
// game.settings ('multi-tokenart')
{
  "hpCurrentPath": "system.attributes.hp.value",  // Путь к текущему HP
  "hpMaxPath": "system.attributes.hp.max"          // Путь к максимальному HP
}
```

Модуль попытается определить систему автоматически при первом запуске (пресеты для dnd5e, pf2e, wfrp4e, и т.д.), с возможностью ручной настройки.

---

## 3. Функциональные блоки

### 3.1 Контекстное меню токена
- Кнопка «Менеджер портретов» (🎭), доступна GM/Owner.
- Hook: `Hooks.on('getTokenActionButtons', ...)`

### 3.2 Основное окно менеджера
- `ApplicationV2`, немодальное, ресайзабельное.
- Заголовок: `Multi Art: [Actor Name]`
- Секции Token/Portrait с random/refresh, glow активного, default label, edit settings.

### 3.3 Автоактивация изображений
Приоритет: customScript > wounded > die. 
- Random отключает автоактивацию.
- Для wounded: выбирать наименьший подходящий порог, при равенстве — случайно.
- Если нет кандидатов: оставить текущее / вернуть default.

### 3.4 Dynamic Token Ring
- При активации изображения с `dynamicRing.enabled` перезаписывать ring.
- Хранить и восстанавливать оригинал (`originalRing`) при деактивации.

### 3.5 Drag & Drop и загрузка
- Поддержка FilePicker и D&D файлов.
- Папка `[Data]/multi-tokenart/[ActorSlug]/` с автосозданием.
- Форматы: webp/png/jpg/jpeg/gif/svg/avif.

### 3.6 Переупорядочивание
- Drag-n-drop внутри секции с обновлением `sort`.

### 3.7 Default-изображение
- Только одно default на тип.
- Для новых токенов: default при random off, random image при random on.

### 3.8 Права доступа
- GM + Owner: полный доступ.
- Observer: без доступа.

## 4. Структура файлов
Используем структуру с `scripts/`, `templates/`, `styles/`, `lang/`, `module.json`.

## 5. Хуки
- `init`, `ready`, `getTokenActionButtons`, `updateActor`, `updateToken`, `createToken`, `renderActorSheet`.

## 6. Визуальный стиль
- Темная тема + золотой акцент + cyan/red состояния.

## 7. module.json
- `id: multi-tokenart`, `esmodules: [scripts/module.mjs]`, `styles`, `languages`, `socket: true`, compat v13.

## 8. Краевые случаи
- Лимит 100 изображений.
- Unicode slugify fallback на actor.id.
- Random приоритетнее default.
- Last write wins.

## 9. Фазы
1) Фундамент
2) UI
3) Базовая логика
4) Автоматизация
5) Расширенные функции
6) Полировка

---

Этот документ является рабочим каноном для текущего репозитория.

- Добавляется пункт **«Менеджер портретов»** (иконка: маска 🎭) в контекстное меню (правый клик по токену).
- Видна **всем**, кто имеет права Owner на актёра.
- При клике открывает основное окно менеджера.
- Хук: `Hooks.on('getTokenActionButtons', ...)` (v13 API).

### 3.2 Основное окно менеджера

**Тип:** `ApplicationV2` (Foundry v13), немодальное, ресайзабельное.  
**Заголовок:** `Multi Art: [Actor Name]`

#### Левая зона — библиотека изображений

Две секции с разделителем:

**Секция «Token»:**
- Заголовок «Token» + чекбокс **Random** + кнопка **🔄 Refresh** (рандом)
- Чекбокс **Auto Rotate** (глобальный, синхронизирован)
- Сетка миниатюр изображений (порядок настраивается drag-n-drop)
- Кнопка **+** (добавить изображение)
- На каждой миниатюре:
  - Иконка 🖊 редактирования (верхний правый угол) — открывает Settings
  - **Glow Effect** (золотой) вокруг активного изображения
  - **Зелёная точка** в углу активного изображения
  - Подпись **«default»** под изображением, если оно помечено как default
- Клик по миниатюре — мгновенно переключает изображение токена

**Секция «Portrait»:**
- Аналогична секции Token
- Свой независимый чекбокс **Random** + **🔄 Refresh**
- НЕТ чекбокса Auto Rotate
- Клик переключает `actor.img`

#### Правая зона — Settings (выезжающая панель)

Открывается при нажатии на 🖊 конкретного изображения. Обновляет контент при переключении на другое изображение.

**Заголовок:** `Settings: Token N` / `Settings: Portrait N` (N — порядковый номер)

**Для Token image:**

| Элемент              | Тип            | Описание                                               |
|----------------------|----------------|--------------------------------------------------------|
| Default Image        | Checkbox       | Помечает как default (одно на тип)                      |
| Auto Enable          | Заголовок      | —                                                      |
| Wounded Enable       | Checkbox       | Включить автоактивацию по HP%                          |
| Wounded %            | Number input   | Порог (1–99)                                           |
| Die (0 HP)           | Checkbox       | Включить при HP <= 0                                   |
| Custom auto enable   | Code textarea  | JS-код условия                                         |
| Dynamic Token Ring   | Заголовок      | —                                                      |
| Enable               | Checkbox       | Перекрывать стандартные настройки кольца                |
| Коррекция масштаба   | Range slider   | Scale correction                                       |
| Цвет кольца          | Color picker   | Ring color                                             |
| Цвет фона            | Color picker   | Background color                                       |
| Image File           | Text + Browse  | Путь к файлу + кнопка FilePicker                       |
| **Del**              | Button (red)   | Удалить запись (с подтверждением)                       |
| **Save**             | Button (gold)  | Сохранить настройки                                    |

**Для Portrait image:**
- Всё то же самое, **кроме**: Auto Rotate, Dynamic Token Ring — не отображаются.

**Поведение при несохранённых изменениях:**  
Если пользователь переключается на другое изображение (клик на другую 🖊) или закрывает Settings, и есть несохранённые изменения — показать диалог: **«Сбросить изменения / Сохранить»**.

**Когда Random включён:**  
- Все настройки кроме глобального Auto Rotate визуально дизейблены (opacity, серый оверлей).
- При попытке изменить — popup-уведомление: *«Настройка недоступна в режиме Random»*.

### 3.3 Система автоактивации изображений

Срабатывает при изменении HP актёра/токена.

**Алгоритм приоритетов (для каждого типа: token / portrait):**

```
1. Если включён Random → ничего не делать (автоактивация отключена)

2. Собрать все изображения с активными условиями

3. Для каждого изображения проверить:
   a. customScript (если есть и возвращает true) → кандидат
   b. wounded (если HP% <= порог) → кандидат
   c. die (если HP <= 0) → кандидат
   
   Приоритет: customScript > wounded > die
   Если у изображения стоит и wounded и die → die игнорируется

4. Из всех кандидатов по wounded:
   → Выбрать тот, чей порог НИЖЕ (ближе к текущему HP)
   → При одинаковых порогах → случайный выбор

5. Из всех кандидатов по customScript:
   → Побеждает customScript (приоритетнее wounded/die)

6. Если кандидатов нет → оставить текущее / вернуть default
```

**Auto Rotate (глобальный):**
- При HP <= 0: текущее изображение токена поворачивается на 270° по часовой стрелке (CSS transform или token.rotation).
- При HP > 0: возвращается к исходному повороту.
- Срабатывает независимо от Random-режима.

### 3.4 Dynamic Token Ring

При активации изображения, у которого `dynamicRing.enabled === true`:
- Модуль перезаписывает параметры `token.ring` стандартного API Foundry.
- При деактивации (переключение на изображение без Dynamic Ring) — восстанавливаются оригинальные значения.

Необходимо хранить оригинальные значения ring при первом перекрытии:
```jsonc
// token.flags['multi-tokenart']
{
  "originalRing": { /* snapshot оригинальных token.ring значений */ }
}
```

### 3.5 Drag & Drop и загрузка файлов

**Кнопка «+»:**
- Открывает стандартный `FilePicker` Foundry.
- Выбранный файл добавляется в библиотеку.

**Drag & Drop:**
- Зоны Token и Portrait принимают перетаскивание файлов.
- Визуальная индикация при наведении (подсветка зоны).
- Файлы загружаются в: `[Data]/multi-tokenart/[ActorName]/`
- Папка создаётся автоматически через `FilePicker.createDirectory()`.
- Поддержка: файлы из ОС, Foundry File Browser.
- Форматы: `.webp`, `.png`, `.jpg`, `.jpeg`, `.gif`, `.svg`, `.avif`

**Имя подпапки актёра:**  
Санитизация имени: `actor.name.slugify({strict: true})` или fallback на `actor.id`.

### 3.6 Переупорядочивание миниатюр

- Drag-n-drop внутри секции (Token ↔ Token, Portrait ↔ Portrait).
- Обновление поля `sort` у затронутых записей.
- Визуальная индикация позиции вставки при перетаскивании.

### 3.7 Default-изображение

- Только **одно** default на тип (token / portrait).
- При установке нового default — старый сбрасывается.
- При установке default **из окна токена на сцене** — значение записывается в `actor.flags` и обновляет `actor.prototypeToken.texture.src` (для token) или `actor.img` (для portrait).
- При размещении нового токена: если Random выключен — применяется default. Если Random включён — применяется случайное.

### 3.8 Права доступа

| Действие                        | GM  | Owner | Observer |
|---------------------------------|-----|-------|----------|
| Открыть менеджер                | ✅  | ✅    | ❌       |
| Переключить изображение         | ✅  | ✅    | ❌       |
| Редактировать Settings          | ✅  | ✅    | ❌       |
| Добавить изображение            | ✅  | ✅    | ❌       |
| Удалить изображение             | ✅  | ✅    | ❌       |
| Изменить глобальные настройки   | ✅  | ✅    | ❌       |

---

## 4. Структура файлов модуля

```
multi-tokenart/
├── module.json
├── lang/
│   └── en.json
├── scripts/
│   ├── module.mjs              # Точка входа, регистрация хуков
│   ├── constants.mjs           # Константы, ID модуля, дефолты
│   ├── settings.mjs            # game.settings регистрация
│   ├── data/
│   │   ├── ImageData.mjs       # Класс данных изображения (foundry DataModel)
│   │   └── ModuleData.mjs      # Корневая модель данных actor.flags
│   ├── apps/
│   │   ├── MultiTokenArtManager.mjs  # Главное окно (ApplicationV2)
│   │   └── SettingsPanel.mjs         # Панель настроек (часть менеджера)
│   ├── logic/
│   │   ├── AutoActivation.mjs  # Логика автопереключения по HP
│   │   ├── DynamicRing.mjs     # Интеграция с Dynamic Token Ring
│   │   ├── AutoRotate.mjs      # Логика поворота при HP <= 0
│   │   └── RandomMode.mjs      # Логика Random-режима
│   ├── ui/
│   │   ├── TokenHUD.mjs        # Кнопка в контекстном меню токена
│   │   └── DragDrop.mjs        # Drag & Drop обработчик
│   └── utils/
│       ├── hp-resolver.mjs     # System-agnostic получение HP
│       └── file-utils.mjs      # Работа с папками и файлами
├── styles/
│   └── multi-tokenart.css      # Все стили модуля
└── templates/
    ├── manager.hbs             # Шаблон главного окна
    ├── settings-panel.hbs      # Шаблон панели настроек
    └── partials/
        ├── image-card.hbs      # Миниатюра изображения
        └── confirm-dialog.hbs  # Диалог подтверждения
```

---

## 5. Хуки и интеграция с Foundry API

### 5.1 Регистрация

```javascript
Hooks.once('init', () => {
  // Регистрация настроек (HP paths)
  // Регистрация DataModel
});

Hooks.once('ready', () => {
  // Определение системы, автозаполнение HP paths
});
```

### 5.2 Контекстное меню

```javascript
// Foundry v13 API
Hooks.on('getTokenActionButtons', (token, buttons) => {
  // Добавить кнопку "Менеджер портретов"
});
```

### 5.3 Автоактивация при изменении HP

```javascript
Hooks.on('updateActor', (actor, changes, options, userId) => {
  // Проверить изменение HP → запустить AutoActivation
});

Hooks.on('updateToken', (tokenDoc, changes, options, userId) => {
  // Аналогично для unlinked токенов
});
```

### 5.4 Размещение токена

```javascript
Hooks.on('createToken', (tokenDoc, options, userId) => {
  // Применить default или random изображения
});
```

### 5.5 Перехват портрета при открытии чарника

```javascript
Hooks.on('renderActorSheet', (sheet, html, data) => {
  // Если открыт через токен — подставить активный портрет
  // Если из сайдбара — показать default/actor.img
});
```

---

## 6. Визуальный стиль (CSS)

### Палитра
| Роль                | Значение                      |
|---------------------|-------------------------------|
| Background          | `#111111`                     |
| Panel               | `rgba(0, 0, 0, 0.4)`         |
| Border              | `1px solid #bd8e34`           |
| Border radius       | `4px`                         |
| Accent Gold         | `#bd8e34`                     |
| Accent Cyan         | `#22d3ee`                     |
| Accent Red          | `#ef4444`                     |
| Active Glow         | `0 0 10px 3px #bd8e34`        |
| Active Dot          | `#22c55e` (green-500)         |
| Disabled overlay    | `rgba(0, 0, 0, 0.5)`         |

### Типографика
| Роль       | Шрифт               | Размер      | Стиль     |
|------------|----------------------|-------------|-----------|
| Headers    | Modesto Condensed    | 13–14px     | uppercase |
| Body       | Signika              | 11–12px     | normal    |

### Элементы
- Кнопки: ghost-стиль → при hover заливка золотом + glow.
- Primary button (Save): золотой фон + glow.
- Del button: красный фон при hover.
- Dropdown/Select: тёмный фон + золотая рамка.
- Чекбоксы: кастомные, золотая рамка, cyan заливка при checked.
- Миниатюры: 80×80px, border-radius 4px, золотая рамка.
- Disabled state: серый оверлей, cursor: not-allowed.

---

## 7. module.json

```jsonc
{
  "id": "multi-tokenart",
  "title": "Multi Token Art",
  "description": "Manage multiple token and portrait images with auto-activation, random mode, and Dynamic Ring integration.",
  "version": "1.0.0",
  "compatibility": {
    "minimum": "13",
    "verified": "13"
  },
  "authors": [
    {
      "name": "Author"
    }
  ],
  "esmodules": ["scripts/module.mjs"],
  "styles": ["styles/multi-tokenart.css"],
  "languages": [
    {
      "lang": "en",
      "name": "English",
      "path": "lang/en.json"
    }
  ],
  "socket": true,
  "url": "",
  "manifest": "",
  "download": ""
}
```

---

## 8. Краевые случаи и заметки

1. **Лимит 100 изображений** — проверка при добавлении, UI-уведомление при превышении.
2. **Удаление актёра** — данные flags удаляются автоматически с актёром.
3. **Unlinked токены** — для них данные хранятся в `token.delta.flags`, работает штатно.
4. **Конфликт Default + Random** — Random приоритетнее, Default игнорируется.
5. **Несколько Default** — при установке нового default старый автоматически сбрасывается (в пределах типа).
6. **Concurrent editing** — при одновременном редактировании несколькими пользователями — last write wins (стандартное поведение Foundry).
7. **Восстановление ring** — при удалении модуля, оригинальные значения ring остаются в token.flags, но ring уже был восстановлен при деактивации.
8. **Имена актёров с Unicode** — slugify с fallback на actor.id.
9. **Socket** — нужен для уведомления других клиентов о смене изображения на чужом токене (права Owner).

---

## 9. Дорожная карта реализации (фазы)

### Фаза 1 — Фундамент
- [ ] `module.json`, точка входа, константы, настройки
- [ ] DataModel для хранения данных
- [ ] HP resolver (system-agnostic)

### Фаза 2 — Основной UI
- [ ] ApplicationV2 окно менеджера
- [ ] Рендер миниатюр (Token + Portrait)
- [ ] Settings-панель (выезжающая)
- [ ] Стили CSS (Dark/Gold тема)

### Фаза 3 — Базовая логика
- [ ] Кнопка в контекстном меню токена
- [ ] Переключение изображения по клику
- [ ] Добавление/удаление изображений (FilePicker + D&D)
- [ ] Default-изображение
- [ ] Сохранение/загрузка данных в flags

### Фаза 4 — Автоматизация
- [ ] Auto-activation по HP (Wounded / Die)
- [ ] Приоритизация при конфликтах
- [ ] Custom script условия
- [ ] Auto Rotate при HP <= 0

### Фаза 5 — Расширенные функции
- [ ] Random-режим
- [ ] Dynamic Token Ring интеграция
- [ ] Drag & Drop переупорядочивание
- [ ] Unsaved changes диалог

### Фаза 6 — Полировка
- [ ] Локализация
- [ ] Права доступа (проверки)
- [ ] Обработка краевых случаев
- [ ] Тестирование на разных системах
